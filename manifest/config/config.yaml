# HTTP Server.
server:
  address: ":8000"
  dumpRouterMap: true
  routeOverWrite: true
  accessLogEnabled: true
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

# Global logging.
logger:
  level: "all"
  stdout: true

# Database.
database:
  logger:
    level: "all"
    stdout: true

  default:
    link: "mysql:root:root@tcp(192.168.162.129:13306)/wy-goframe-admin?loc=Local"
    debug: true


# Redis
redis:
# 单实例配置示例1
  default:
    address: 192.168.162.129:6379
    db:      0
    pass: "canpanscp" 